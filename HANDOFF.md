# 引き継ぎメモ（2026-09-09 時点）

別のAI・別の担当者が続きを行うための現状整理。

> **先に `CLAUDE.md` を読むこと。** 問題を作る・直すときの決まり（鉄則7つ）と、
> これまで実際に踏んだ失敗が書いてある。同じ失敗を繰り返さないために必要。
>
> 仕様は `docs/仕様書.md`。特に **§13「問題の品質基準」** が中核。

---

## このプロジェクトは何か

AWS認定資格の学習ウェブアプリ。静的HTML1枚（`index.html`）＋データファイル（`data/*.js`）で動く。
サーバー不要。`index.html` をブラウザで開けば使える。

公開先: https://t03-77.github.io/quiz-demo-x7k2m/
（`git push origin main` で反映。認証は保存済み）

**利用者の目的は「試験に合格すること」。** 本番より易しい問題は、どれだけ数を揃えても価値がない。
この一点がすべての判断基準になる。

---

## データの二層構成（重要・著作権対応）

| 種別 | 場所 | サイトに同梱 |
|---|---|---|
| オリジナル問題 1539問（自作） | `data/orig.js` | **する** |
| AWS公式問題 1290問 | `資料/変換済み/questions_all.json` | **しない**（利用者が手元のファイルを読み込む方式） |
| 読み上げ音声（自作問題ぶん） | `audio/aip-c01-orig_*.mp3` | **する**（2026-08-28〜。著作権の制約がない） |
| 読み上げ音声（公式問題ぶん） | `audio/aip-c01_*.mp3` ほか | **しない** |

`資料/` は `.gitignore` 済み。**この方針は崩さないこと。**

`audio/` は**ファイル名で選り分けている**。`audio/*` で全部除外したうえで、
`!audio/*-orig_*.mp3` だけを例外にしてある（`audio/` とディレクトリごと除外すると
中を探索しないため例外が効かない）。**新しく音声を作るときは命名規則に従うこと。**
自作問題ぶんは `-orig_` を必ず入れ、公式問題ぶんには入れない。

**公式問題を改変したものをサイトに載せてはいけない**（翻案にあたる）。
論点を参考にして別のシナリオで作るのは問題ない（アイデアは著作権の保護対象外）。

---

## 現在の到達状況

### できていること

| 項目 | 状態 |
|---|---|
| 問題数 | **1539問**（全11資格。各105〜381問。うち約94問は2026-09-03〜09 に論点ギャップから新規作成） |
| 出題の形 | 1正解4肢 / 2正解5肢 / 3正解6肢 + マッチング15・並び替え15 |
| 問題文の分量 | 全11資格で公式模試の下位10%を下回る問題が **0問** |
| 解説の分量 | 全11資格が公式水準に到達 |
| ドメイン配分 | 全11資格が公式試験ガイドの比重と整合 |
| 当てやすさ | 「正解が最長」を9資格で公式並みに（例: DOP-C02 99%→17%） |
| 正解の位置 | データ上はAに82%偏るが、**出題時はシャッフルで A23/B22/C22/D25** |
| 事実の正確性 | 書き換えた誤答2271個のうち、裏取りが要る327件を検証し誤りは1件のみ。**AIP-C01 は別途、全問を公式ドキュメントで精読して60箇所超を是正**（下記） |
| 機能 | 演習・模擬試験・SRS・弱点分析・図解・用語集・音声・AI連携・PWA（デモモードは2026-08-31に役目を終えて削除） |
| テスト | E2E 27本すべて合格（2026-09-07 に全件実行して確認） |
| AIP-C01 | **完了**（2026-09-07）。機械検査の全観点で公式レンジ内、精読で誤り是正、論点ギャップ解消、strong率が公式と同水準 |

### できていないこと（品質差の実測・2026-09-01 に再測定）

**ここが最重要。** ただし下表の古い数値のうち2つは、2026-09-01 の再測定で解消済みと判明した。

| 指標 | 公式 | 自作(旧) | 自作(2026-09-01) | 状態 |
|---|---|---|---|---|
| 正解と1点だけ違う誤答がある (O-3) | **資格別** | 5.2% | **DEA/CLF/SOA/DVA/DOP/AIP/MLA/AIF 是正済✓ / SAP は不要と判定** | 42.3%は9/1の古い値。**資格別に見ること**（下記1.） |
| 「手動」が正解になる (W-6) | 0%※ | 0.8% | **0%（1問）** | audit_strawman の現定義では公式と同水準。※旧11.8%は別の広い定義での測定値で、要再検証 |
| 2×2マトリクス構造 (O-2) | 14.6% | 0.4% | 未再測定 | 正解肢の組み直しが必要（鉄則3の手順） |
| 設定断片（JSON/ログ）を提示 (S-9) | 1.1% | 0.1% | **着手済み** | 2026-09-03 に SAA-C03 で8問作成。公式の該当は11問(1.1%)で、旧記載の5%は過大だった |
| **肢どうしの語の重なり** | 資格別 | — | **資格別16件が未達** | `audit_pattern.py` 参照。最不足: DOP 43% / SAP 51%充足。**2026-09-01 に第1弾着手（DOP/SAP/DEA 各30問）** |
| **選択肢が2文以上** | 資格別 | — | SCS 11% / DEA 27%充足 ほか | 正解肢の構造変更が必要（誤答だけでは動かないことを実測済み） |

**ブラインド消去法テスト（2026-09-03 再実施）**: 公式40問＋自作40問を出典を伏せて同一判定者で判定した結果、
公式90% / 自作100%（guess除き 82.5% / 97.5%）。**同一実行内の差は10〜15pt** まで縮小した。
ただし今回の判定者は公式問題も9割解けており測定が天井に張り付いている。
8/27 の「公式46% / 自作83%」とは判定者・基準が異なり**直接比較はできない**（鉄則5）。
測り方の改善案を含む詳細は `資料/生成/_ブラインド消去法_20260903.md`。
あわせて D1型（仕様上できない断定）の誤答60肢をファクトチェックし、**事実誤りゼロ**
（断定が古い解説2件のみ修正）を確認した。

---

## 残作業（優先度順）

### 0. 誤答肢の書き直し（**完了・2026-08-29**）

> **この作業は終わっている。再開する必要はない。**
> DVA-C02 48問すべてを書き直し、最長の肢が正解である割合を公式の分布に合わせた。
>
> | | 公式 | 現在 |
> |---|---|---|
> | DVA-C02 最長が正解 | 23.5% | **20.6%** |
> | DVA-C02 最長が誤答 | 74.1% | **72.5%** |
> | MLA-C01 最長が正解 | 21.0% | **26.0%** |
> | MLA-C01 最長が誤答 | 73.9% | **71.2%** |
>
> `audit_all.py` の「当てやすさ」もOKになっている。
>
> **MLA-C01 に残る24問は直してはいけない。** 「正解が最長」の問題数だけ見ると
> まだ24問あるが、分布で見ると既に公式並みで、直すと逆に振れる。
> 一度 DVA-C02 でこの失敗をしている（下記）。

以下は手順の記録（他の資格で同じ症状が出たときに使う）。

**やり方が確立したので、まずこれを続けるのが効率的。**

短い誤答を、正解と要素を共有した切りにくい誤答に置き換える。**正解肢は触らない。**
短い誤答は同時に質も低い（「手動で突き合わせ」「問題文が否定済みの方法」など、
中身を知らなくても切れるもの）ため、長さを揃えることが質を上げることと一致する。

手順:
1. `python -X utf8 tools/audit_difficulty.py` で「正解が最長」が公式より高い資格を選ぶ
2. 対象問題を抽出（下のコマンド）
3. パッチJSONを書く → `python tools/{資格}_apply_patch.py <patch> --dry` で長さを確認
   - **各問で誤答を1つは正解より長くする**。同値でも「正解が最長」の判定に引っかかる
4. 適用 → `build_orig_data.py` → `smoke_test.js` と `audit_consistency.py`
5. 10問ごとにコミット

```
python -X utf8 -c "
import json
js=open('data/orig.js',encoding='utf-8').read(); n=json.loads(js[js.index('['):js.rindex(']')+1])
for ex in ['DVA-C02','MLA-C01']:
    qs=[q for q in n if q.get('exam')==ex and q.get('set')=='orig' and q.get('options')]
    hit=[q['id'] for q in qs if min(len(o['text']) for o in q['options'] if o['correct']) > max(len(o['text']) for o in q['options'] if not o['correct'])]
    print(ex, len(hit), '問'); json.dump(hit, open('資料/生成/_longest_%s.json'%ex,'w'), ensure_ascii=False)
"
```

**やりすぎに注意。** 下げれば下げるほど良いわけではない。
DVA-C02 で 58%→11% まで下げたところ、最長の肢が正解である割合が 4.9% になり、
公式 23.5% を大きく下回った。「一番長い肢を捨てれば当たる」という逆の手がかりになる。
公式の値に**合わせる**こと。測り方は CLAUDE.md 鉄則4を参照。

> **2文以上（40% vs 23%）はこの作業では増えない。** 公式で多いのは
> 正解肢自体が「操作A。操作B。」の2文構造だから。誤答だけ直しても動かないことを
> 実測で確認済み（`資料/生成/_予測_長さと構造_2026-08-28.md`）。
> 着手するなら鉄則3の手順で正解肢を含めて組み直すこと。

### 0b. 問題文の分量（**完了・2026-08-31。audit_all の要確認は 0件になった**）

> **この作業は終わっている。** `audit_all.py` は機械で測れる観点すべてが基準内（exit=0）。

| 資格 | 公式比 | 状態 |
|---|---|---|
| CLF-C02 | ~~147%~~ → **107%** | **完了（2026-08-30）**。100字以上の63問から、正解の決め手になる制約語を残して枕・冗長表現だけを削った。中央値 109→79.5字 |
| AIF-C01 | ~~82%~~ → **97%** | **完了（2026-08-31）**。100字未満の50問に「誤答を選ばせる圧力になる文脈」（誤答のやり方が社内で案に挙がっている等、鉄則2準拠）を1〜2文追加。中央値 100→117字。orig_089 の誤字「顧客side」も修正 |
| MLA-C01 | ~~84%~~ → **93%** | **完了（2026-08-31）**。170字未満の40問に同方針で追加。中央値 177→196字 |

パッチ・予測と実測の照合・修正前ソースは `資料/生成/` の
`_patch_{clf,aif,mla}_len_*.json` / `_予測_*長さ_*.md` / `_bak_*_len_*/`。
**単なる水増しはしていない**（追加した文は誤答を魅力的にする状況説明に限定。
キーワード直結・当てやすさの検査で悪化なしを確認済み）。

### 1. O-3: 正解と1点だけ違う誤答（**増やす作業は終了。いまは「減らす」段階**）

> 測定は SequenceMatcher 類似度 ≥0.72 の誤答が1つ以上ある問題の割合。
>
> **2026-09-09 に資格別で測り直したところ、11資格すべてが公式を超えていた。**
> ここに書いてあった「42.3%（公式30.5%）」は 9/1 の全資格平均で、その後
> 9/1〜9/3 の語の重なり作業（「正解肢をコピーして1〜2トークン置換」）が
> **O-3 を機械的に押し上げていた**。全体は 55.4%（公式30.2%）まで上がっていた。
>
> **公式値は資格ごとに6倍以上違う**（CLF-C02 9.2% / SAP-C02 61.3%）。
> 全資格平均と比べてはいけない（鉄則5）。

| 資格 | 公式 | 自作 | 状態 | 資格 | 公式 | 自作 | 状態 |
|---|---|---|---|---|---|---|---|
| **DEA-C01** | 40.0% | **40.4%** | **是正済** | **MLA-C01** | 31.7% | **35.3%** | **是正済** |
| SAA-C03 | 26.2% | 62.7% | 超過(最大) | SCS-C03 | 30.6% | 60.6% | 超過 |
| **CLF-C02** | 9.2% | **10.5%** | **是正済** | **SOA-C03** | 24.6% | **26.2%** | **是正済** |
| **DOP-C02** | 53.3% | **56.8%** | **是正済** | SAP-C02 | 61.3% | 74.3% | 超過 |
| **DVA-C02** | 36.9% | **36.8%** | **是正済** | AIP-C01 | 20.5% | 32.5% | 超過 |
| **AIF-C01** | 17.2% | **19.3%** | **是正済** | | | | |

**DEA-C01 は 2026-09-09 に是正済み**（66.3%→40.4%）。27問の双子肢を1肢ずつ
D4（別機能）/ D6（粒度が粗い）の誤答に差し替えた。分布も公式に寄った
（中央 0.824→0.648、公式 0.625。突出度 0.253→0.148、公式 0.134）。
予測・実測・手順は `資料/生成/_予測_dea_o3_20260909.md`。
専用ツール: `tools/_deao3_{lib,snapshot,verify,try,apply}.py`（他資格にも流用可）。

**CLF-C02 と SOA-C03 も 2026-09-09 に是正済み**（CLF 40.4%→10.5%、公式9.2%。34問36肢。
SOA 55.4%→26.2%、公式24.6%。38問38肢）。予測・実測は
`資料/生成/_予測_clf_soa_o3_20260909.md`、バックアップは `資料/生成/_bak_clf_soa_o3_20260909/`。
専用ツール: `tools/_o3b_{lib,snapshot,verify,dump,show,try,apply,tok,cand}.py`（資格を引数で渡す）。
**`_o3b_tok.py` と `_o3b_cand.py` は残り8資格でそのまま使うこと**（下記4.）。

**DVA-C02 と DOP-C02 も 2026-09-09 に是正済み**（DVA 64.0%→36.8%、公式36.9%。31問31肢。
DOP 79.7%→56.8%、公式53.3%。27問32肢＝22問×1肢＋5問×2肢）。予測・実測は
`資料/生成/_予測_dva_dop_o3_20260909.md`、バックアップは `資料/生成/_bak_dva_dop_o3_20260909/`。
専用ツール: `tools/_dvdo3_{lib,dump,try,apply,snapshot,verify}.py`（`O3EXAM` か第1引数で資格を渡す）。
語の重なりは DVA 0.295→0.275（公式0.286・下限0.2578）、DOP 0.431→0.408（公式0.438・下限0.3946）。
**DOP は公式値ちょうどまで下げなかった。** 残る2肢書換の候補は 2肢とも直しても 0.72 を切らず、
無理に離すと重なりの残り 0.013 を使い切るため、56.8%（目標レンジ 40〜67%内）で止めている。

**MLA-C01 と AIF-C01 も 2026-09-09 に是正済み**（MLA 59.5%→35.3%、公式31.7%。28問28肢。
AIF 24.6%→19.3%、公式17.2%。6問6肢）。予測・実測は
`資料/生成/_予測_mla_aif_o3_20260909.md`、バックアップは `資料/生成/_bak_mla_aif_o3_20260909/`。
専用ツール: `tools/_mao3_{lib,dump,cand,try,apply,snapshot,verify}.py`（`O3EXAM` か第1引数で資格を渡す。
`_mao3_cand.py` は候補文を一括採点し、**消えた共有語まで表示する**）。
語の重なりは MLA 0.262→0.252（公式0.271・下限0.2435）、AIF 0.173→0.171（公式0.157・**上限0.1732を超えていたのが解消**）。
`_mao3_apply.py` には `mixed_orig_b1.json`（MLA 3問が他資格と同居）への**id 単位の書込禁止**を足してある。

**AIF は「pt 差が小さいから不要」と判断しかけたが、比で見ると外れていた。**
公式 17.2% に対し +7.4pt は SAP-C02（+13pt・見送り）より小さいが、
**比は 1.43 で目標レンジ (x0.75〜x1.25) の外**。公式値が低い資格ほど pt 差は小さく出る。
**判断は比で行うこと。**

**他資格に着手する前に必ず読むこと:**
1. **肢どうしの語の重なりを先に測る。** O-3 を下げると重なりは必ず下がる。
   DEA は 0.332→0.317 で★不足の下限(0.3143)まで詰まり、これ以上下げられない。
   重なりに余裕がない資格（公式値との差が小さい資格）は、下げ幅を制限すること。
2. **書き換える肢に、他の誤答肢と共通する語彙を意図的に残す。**
   素朴に別サービスの誤答へ差し替えると重なりが1問あたり 0.090 落ちるが、
   語彙を共有させると 0.055 まで半減する（DEA で実測）。
3. **ゼロにしない。** 公式も 0.80 以上の双子を DEA で34%持っている。
   減らしすぎれば「似ていない肢が正解」という逆の手がかりになる（鉄則4）。
   **どの双子を残すかを着手前に列挙すること。** CLF/SOA では「その資格の核心を問う対比」
   （ゲスト OS とホスト OS、RPO と RTO、DeletionPolicy と UpdateReplacePolicy 等）と
   2×2 マトリクス構造の問題を温存枠にし、残りを書き換える設計にした。
4. **語彙を残す作業は勘でやると外れる。`_o3b_cand.py` で採点しながら書くこと。**
   SOA の初稿は「語彙を残したつもり」で 1問あたり −0.115 も重なりを落とし、
   20問で下限に到達した。`_o3b_tok.py` で見ると原因は
   (1) 元の肢が他肢と共有していた語を落とす (2) その肢にしかない語を持ち込む の2つだけ。
   候補を2〜3案書いて `_o3b_cand.py` で採点する運用にしたら **−0.024/問** まで下がり、
   38問書き換えても 0.245→0.236 で収まった。
   **重なりは語の集合、O-3 は文字列の並びで測る。**
   語彙を保ったまま語順と構文を変えれば、重なりを落とさずに O-3 だけ下げられる。
   DVA/DOP でも同じで、**節の順序を入れ替える・同じ末尾の一文をそのまま残す**のが効いた
   （初稿 −0.120/問 → −0.05〜0.06/問）。
5. **書き換えたら `audit_strawman.py` の keyword_leak を必ず通す。**
   問題文の特徴語が全誤答肢から消えて正解肢だけに残る「キーワード直結」が
   DOP で2問（`_063` の `Amazon CloudWatch Logs`、`_109` の `AWS RAM`）新しく生まれた。
   書き換えた肢に語を戻すだけで解消するが、測らないと気づけない。

### 1b. 肢どうしの語の重なり（**完了・2026-09-03。全11資格が公式レンジ内**）

> **この作業は終わっている。** `audit_pattern.py` の資格別表で★（不足・超過）が
> **すべて消えた**。これ以上いじらないこと（超過も欠陥になる）。

| 資格 | 公式 | 現在 | 資格 | 公式 | 現在 |
|---|---|---|---|---|---|
| DOP-C02 | 0.438 | 0.430 | DVA-C02 | 0.286 | 0.295 |
| SAP-C02 | 0.409 | 0.397 | MLA-C01 | 0.271 | 0.262 |
| DEA-C01 | 0.349 | 0.332 | SOA-C03 | 0.257 | 0.245 |
| SCS-C03 | 0.285 | 0.276 | AIP-C01 | 0.247 | 0.273 |
| SAA-C03 | 0.246 | 0.243 | AIF-C01 | 0.157 | 0.173 |
| CLF-C02 | 0.119 | 0.142 | | | |

効いた方法は **「正解肢の文をコピーして1〜2トークンだけ置換する」**（9/1〜9/3 の4巡で
延べ400問超を組み直した）。意味的に骨格を似せるだけでは指標が動かないことを実測済み。
逆に AIF-C01 は公式より高すぎたため、誤答から共通語を削って**下げる**作業を行った。

手順と失敗の記録: `_予測_overlap2_20260901.md` 〜 `_予測_overlap4_20260903.md`

**組み直しの副作用は必ず出る。** 実際に起きたもの:
「最長は誤答」の癖 / 「最長が正解」の上振れ / キーワード直結の新規発生 / 制約の裏返しの新規発生。
**適用後は必ず `audit_all.py` を回す**（語の重なりは 9/3 から audit_all に統合済み）。

### 1c. 選択肢が2文以上（**完了・2026-09-09。全11資格が公式レンジ内**）

最後まで残っていた本丸。7資格を並行で組み直して全資格が公式水準に到達した。

| 資格 | 公式 | 自作 | 資格 | 公式 | 自作 |
|---|---|---|---|---|---|
| DEA-C01 | 49% | 49% | SAA-C03 | 20% | 20% |
| DOP-C02 | 80% | 80% | AIF-C01 | 2% | 2%（超過を解消） |
| SAP-C02 | 84% | 82% | SOA-C03 | 39% | 39% |
| DVA-C02 | 40% | 41% | | | |

**やり方は資格ごとに違う。** 詳細は CLAUDE.md 鉄則5「直し方も資格ごとに選ぶ」に書いた。要点:

- **既定は分割方式**（連用中止を句点に替える。「XをAし、YをBする。」→「XをAする。YをBする。」）。
  語彙集合を動かさないので**語の重なりが原理的に変わらない**。5資格がこれで達成し重なりは完全不変
- **加筆方式（SCS-C03 で使った手）は重なりを必ず下げる。** SAP の担当が dry-run で検出して差し戻した。
  SCS で使えたのは SCS の重なりに上げ代があったからで、他資格には流用できない
- 組み直すと**「最長が正解」が問単位で反転する**（DEA 4問・DOP 1問）。dry-run で確認してから適用すること

**ただし指標が緑になっただけで、公式の書き方には届いていない。** SAP の文数分布は
1文80 / 2文355 / 3文11 に対し、公式は 1文48 / 2文103 / 3文95 / 4文41 / 5文以上26。
**公式の3文以上52%に対し自作は2%**で、「2文以上か否か」という二値の指標では検出されない。
ここを詰めるなら加筆が要るが、そのときは第2文が導入する語を全肢で同一にする書き方になる
（SAP 担当が手順を `資料/生成/_2s_sap/_不採用_加筆方式/` に残している）。

### 1c-2. 他資格の精読（**着手・2026-09-09**）

AIP-C01 で誤りが80箇所近く出たので、他資格にも同型が眠っているとみて着手した。

**AIF-C01（114問）完了。10件を是正**。内訳は「陳腐化した断定」5件・
「列挙の抜け」1件・その他4件で、**AIP と同じく型1（陳腐化した断定）が最多**だった。

| id | 誤っていた断定 | 実際 |
|---|---|---|
| 131 / 133 | Comprehend は画像を直接読み取れない | カスタムモデルは JPEG/PNG/TIFF・スキャンPDF を入力できる |
| 039 | Amazon Lex に RAG の仕組みは含まれない | 組み込みインテント `AMAZON.QnAIntent` がある |
| 018 / 138 | エンドポイントは常時起動している | Application Auto Scaling でゼロまでスケールイン可能 |
| 068 / 050 | 一定期間のコミットメントと引き換えに確保 | コミットメントなしを含む3種・MU 数の時間課金 |
| 021 | Model Monitor のモニタータイプは3つ | **4種**（バイアスドリフトが抜けていた） |

**型2（変更不可制約）は AIF には該当なし。** マネージドサービスの設定変更を扱う問題が
そもそも無かった。**資格によって出る型が違う。**

**全資格横断で「陳腐化した断定」を調べた結果**（3,701文を抽出し612文を確認）:
`data/orig.js` からさらに3件（AIP-C01_orig_357 / SAA-C03_orig_301 / DEA-C01_orig_045）。
この型は**このプロジェクトで最多**で、通算9件になった。

**未着手**: SAA / SOA / DEA / DVA / DOP / SCS / SAP / CLF の8資格（MLA は作業中）。
**`data/glossary.js`（260語・問題の94%に登場）も調査中**。

### 1d. いま残っている乖離（2026-09-09 実測）

`audit_all` の要確認は1件で、**資格ごとの乖離は全11資格で0件**。残るのは全資格まとめた観点2つ:

| 観点 | 公式 | 自作 | 意味 |
|---|---|---|---|
| 重なりが薄い問題(<0.1) | 19% | 6% | 公式には「肢どうしが全く似ていない問題」が2割ある。自作は全問そこそこ似せている |
| 転換表現あり（解説） | 77% | 91% | 解説の「ただし」「一方」が公式より多い |

**どちらも資格別に見ていない**ので、着手するならまず `audit_pattern.py` に資格別の内訳を足すこと
（鉄則5「資格をまたいで基準を作らない」。これは過去に3回踏んでいる）。

### 2. W-6: 「手動＝誤答」のメタ規則（**再検証済み・優先度低**）

2026-09-01 に再測定した。定義別の結果:

| 定義 | 公式 | 自作 |
|---|---|---|
| audit_strawman.py（狭い: 手動対応が正解） | 0%（1問） | 0%（1問） |
| 広い定義（正解肢に 手動/自前/独自/カスタム/Lambda関数を作成 系の語） | **2.1%**（27問） | **0.6%**（7問） |

旧記載の「公式11.8%」は再現できず、過大だったと判断する。
広い定義でも差は 1.5pt しかなく、「手動と書いてあれば誤答」の抜け道は公式でも 98% 成立する。
直すなら正解をカスタム実装型に変える問題を10〜15問作ることになるが（鉄則3の手順が必要）、
**費用対効果が低いため後回し。** 語の重なり（1b）を先に進めること。

### 3. AIP-C01の「1正解5肢」80問を4肢にする（**完了・2026-08-28**）

> **この作業は終わっている。** 2026-08-30 の計測で、全資格の「1正解5肢」は 0問
> （`audit_all.py` の選択肢の数の分布も公式並みでOK）。
> 手順の記録は `tools/apply_5opt_trim.py` と `資料/生成/_5opt_delete_map.json`。

### 4. 論点ギャップを埋める新問の追加（**着手済み・2026-09-03**）

論点分析（全11資格・完了済み）で見つかった未カバー論点に対し、**56問を新規作成した**。

| 資格 | 問数 | 埋めた論点 |
|---|---|---|
| SOA-C03 | 12 | Lambda運用一式 / Route 53 ルーティング使い分け / CloudFormation 更新失敗系 |
| DVA-C02 | 12 | クライアント側暗号化 / Step Functions アクティビティ / **RCU・WCU 計算**（自作初） |
| CLF-C02 | 12 | EC2購入オプション / AWS CAF / 移行サービス。**オンデマンド・専用ホストが正解**の問題を含む |
| AIF-C01 | 12 | Textract・Rekognition / 回帰指標 MAE・MAPE / Audit Manager |
| SAA-C03 | 8 | **S-9型**（IAMポリシー・NACL・CFn・ログ・フローログを読ませる設定断片問題） |

作成ファイルは `{EXAM}_orig_topic1.json` と `SAA-C03_orig_s9.json`（既存ファイルは無変更）。
**新問は `python tools/_verify_newq.py` で検証する**（id衝突・domain新設・重なりの作りすぎ・
分量の乖離・キーワード直結を一括チェック。設定断片問題は公式の断片問題と比較する）。

**新問作成の落とし穴（9/3 に実際に踏んだ）**: 既存改善用の「コピー＋1〜2トークン置換」を
そのまま新問に適用すると、重なりが公式を大きく超える（SOA で 0.382 / 公式0.257）。
新問には必ず「1問あたりコピー方式は1つまで」「公式の分布に合わせて散らす」と指示すること。

残りの未カバー論点は SOA 76 / DEA 54 / SAP 43 / MLA 42 / DOP 35 / SCS 32 / DVA 21 など（**AIP-C01 は解消済み**）。
`_topic_gap_{EXAM}.md` の補強優先順から続けること。

### 4b. AIP-C01（**完了・2026-09-07**）

利用者が先に受験する資格のため、AIP-C01 だけ全観点を仕上げた。到達点:

| 観点 | 公式 | 自作 | 
|---|---|---|
| 肢どうしの語の重なり | 0.247 | 0.259 |
| 選択肢が2文以上 | 82% | 96% |
| 問題文の分量（中央値比） | — | 97% |
| 最長が正解 | 27% | 27% |
| strong率（ブラインド消去法） | 73% | 73% |

- **問題数 381問**（4択154問 + 一問一答221問 + マッチング/並び替え6問）
- **全問を公式ドキュメントで精読**し、誤りを70箇所近く是正。うち1件は存在しない設定値（chunkingStrategy の「カスタム」）の捏造で、残りは仕様変更への追随漏れと変更不可制約の見落とし
- 論点ギャップ55件を新問54問（topic1/2/3/4）で解消。**見送っていた4論点も 2026-09-09 に作成した**
  （RCP と SCP の使い分け / 評価ジョブへの推論応答の持ち込み / 人による評価ジョブの構成 /
  API Gateway 非プロキシ統合。見送り理由のうち「翻案に近い」は過剰な自己規制で、
  論点を参考に別シナリオで作るのは問題ない）
- **一問一答221問の全体解説を中央51字→130字に充実**（2026-09-09）。書き足す過程で
  事実誤りが**さらに8件**見つかった（Prompt Management にエイリアスはない・
  スロットルされた呼び出しは Invocations に計上されない・A2I の自動起動は
  Textract/Rekognition 限定・埋め込みモデルは UpdateKnowledgeBase で変更できない 等）

**strong率を下げたのは何だったか**（2026-09-06 の実測。`_ブラインド_AIP_推移_20260906.md`）:
「双子の肢」の是正は **-3pt** にしかならず、効いたのは**制約設計の是正で -14pt**。
判定者は「制約との矛盾で切れた」を最多理由に挙げていたのに、私は測りやすい双子に
先に飛びついた。**原因を数えてから手を打つこと**（CLAUDE.md の失敗記録）。

**「変更不可制約の見落とし」という誤りの型**（2026-09-07 に発見）:
Bedrock ナレッジベースのチャンキング設定とパーサーは**データソース作成後に変更できない**
（`API_agent_UpdateDataSource` に明記）のに、「設定を変更して再同期する」という
成立しない手順を書いていた問題が**7問**あった。既知の40箇所は「仕様変更への追随漏れ」
だったが、これは**機能の存在は裏取りしたが、既存リソースに後から適用できるかまでは
見ていなかった**という別の型。**他資格にも同型が眠っている可能性が高い**ので、
新問を作るときは「その設定は後から変えられるか」まで確認すること。

**7問のうち3問は、作成時に MCP で裏取りしたはずの新問だった。**
作成時の裏取りは「自分が書こうとしていること」の確認になりがちで、書かなかった前提
（変更可否・値の一覧）は視野に入らない。651 では `chunkingStrategy` に
**存在しない値「カスタム」**まで書いていた（実在は NONE/FIXED_SIZE/HIERARCHICAL/SEMANTIC）。
**新問も既存問と同じ精読ラウンドに通すこと。** `_verify_newq.py` は機械的検査で、
事実の正確性は見ていない。

### 5. その他

- AIP-C01以外の読み上げ音声（1資格あたり3〜4時間）
- AI機能を実際のAPIキーで未検証（テストはすべて模擬応答）

---

## 作業の手順

### 問題を作る・直す前に

1. `CLAUDE.md` を読む
2. `資料/作問ガイド/{EXAM}.md` を読む（なければ `python tools/make_exam_guide.py {EXAM}`）
3. `資料/生成/_review_criteria_v2.md`（58項目のチェックリスト）を読む

### 直したあとに必ず

```
python tools/build_orig_data.py      # 資料/生成/*.json → data/orig.js
python tools/audit_all.py            # 全観点を一括実行
python tools/audit_criteria.py       # チェックリスト58項目の充足状況
```

個別の検査:

| コマンド | 見るもの |
|---|---|
| `node tools/smoke_test.js` | ID重複・スキーマ |
| `python tools/audit_consistency.py` | 解説と正誤フラグの矛盾 |
| `python tools/audit_domains.py` | ドメイン配分 |
| `python tools/audit_explanations.py` | 解説の分量 |
| `python tools/audit_difficulty.py` | 当てやすさ |
| `python tools/audit_content.py` | 重複・出題の形・古い情報・偏り |

### E2Eテスト（27本）

```
cd %TEMP% && mkdir quizapp_e2e && cd quizapp_e2e
npm init -y && npm i playwright-core
copy <プロジェクト>\tests\* .
node test.js
```

`chromium.launch({channel:'msedge'})` で動く（ブラウザの別途インストール不要）。

**`answer.js` が共通ヘルパー。** 出題はマッチング・並び替え・複数選択も混ざるため、
`.opt` を1つクリックする前提のテストは失敗する。必ず次を使うこと:
- `waitQuestion(page)` … 形式を問わず問題の表示を待つ
- `answer(page, correct)` … 形式を問わず回答して採点画面まで
- `choose(page)` … 回答だけ（模試は途中で採点画面を出さない）
- `startChoiceQuestion(page)` … 選択式限定の機能を試すとき

---

## ファイル構成

```
index.html                     アプリ本体（1枚で完結）
sw.js / manifest.webmanifest   PWA
data/
  exams.js                     試験マスタ（問題数・時間・合格スコア）
  orig.js                      同梱するオリジナル問題（ビルド生成物・直接編集しない）
  glossary.js                  用語集260語
  diagrams.js                  図解14種（Mermaid）
  audio_tracks*.js             音声トラック定義
docs/仕様書.md                  仕様（§13が品質基準）
CLAUDE.md                      作業の決まり（鉄則7つ）
tools/                         ビルド・検査・生成スクリプト
tests/                         E2Eテスト27本
資料/                           .gitignore対象
  変換済み/questions_all.json    公式1290問（比較の基準）
  生成/*.json                    問題のソース（ここを編集してビルドする）
  作問ガイド/{EXAM}.md            公式全問から作った基準
  生成/_review_criteria_v2.md    チェックリスト58項目
```

---

## 環境メモ

- Windows。**日本語を含むファイルの書き込みに PowerShell の `Set-Content` を使わない**（文字化けする）
- PowerShellのヒアドキュメント内でPythonコードを書くと引用符で壊れやすい。複雑な置換はエディタで行う
- Python は `python`、Node は `node`。`$env:PYTHONIOENCODING="utf-8"` を付けないと文字化けする
- `python ... | Select-Object -First 3` のように出力を絞ると、それ以上出力した時点でプロセスが落ちる
