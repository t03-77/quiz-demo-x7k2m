# このプロジェクトで作業するときの決まり

AWS認定資格の学習アプリ。**利用者の目的は「試験に合格すること」**であり、
問題が本番より易しければ、どれだけ数を揃えても価値がない。

最初に `HANDOFF.md`（現状と未完了の作業）を読むこと。

> ここに書いてあるのは**すべて実際に失敗した記録**であり、一般論ではない。
> 新しい失敗をしたら追記し、**効果がないと分かったルールは削除すること**。
> 守ること自体が目的になったルールは、作業を遅らせるだけで害になる。
> （実際、「正解は絶対に変えない」「サブエージェントを多数起動しない」の2つは
> 害があると分かったため見直した。経緯は各項に残してある）

---

## 鉄則1: 問題を作る前に、必ず作問ガイドを読む

```
資料/作問ガイド/{EXAM}.md      ← 公式問題を全問走査して作った基準
```

無ければ `python tools/make_exam_guide.py {EXAM}` で生成する。

ガイドには次が入っている。**実例より先に「守るべき数値」を見ること。**

- 出題の形（正解数 × 選択肢数）と、その比率
- 問題文・選択肢の長さの分布
- 評価軸（「最も運用負荷が低い」など）の使われ方
- **公式が誤答に使っているサービス・機能の一覧**（誤答の材料表）
- 問題文で使われている制約表現の実例
- 型が偏らないよう選んだ実例12問

### やってはいけないこと（実際にやって失敗した）

**公式問題を8問だけ見て100問作る。** 1290問持っているのに88問しか使わず、
「試験範囲が狭い・内容が簡単」と指摘された。ガイドは全問から作ってある。

---

## 鉄則2: 誤答から設計する。正解から作らない

自作問題の欠陥はすべて**作問の順序**が原因だった。

「正解を決める → 説明する問題文を書く → 誤答を後から足す」の順で作ると、必ずこうなる:

| 症状 | 実測 |
|---|---|
| 正解が最長になる | DOP-C02で **99%**（公式14%） |
| 誤答がストローマンになる | 「人手でやる」「手作業で集約」が最頻 |
| 制約が飾りになる | 誤答を1つも落とさない制約が半分以上 |

公式は逆で、**誤答が主役**。「この誤答を魅力的に見せるにはどんな状況が要るか」から設計されている。

### 誤答は次の6型のどれかに必ず分類できること

| 型 | 内容 |
|---|---|
| D1 | 実在するが、仕様上その操作ができない |
| D2 | 動くが、要件を1つだけ落とす |
| D3 | 動くが、指定された軸（コスト・運用負荷など）で劣る |
| D4 | 同一サービスの別機能で、目的が違う |
| D5 | 設定は正しいが、挙動・順序が逆 |
| D6 | タイミングが事後的・粒度が粗い |

**どれにも分類できない誤答は欠陥。**「人手でやる」「何もしない」はAWSの知識がなくても切れる。

詳細なチェックリストは **`資料/生成/_review_criteria_v2.md`（58項目・確定版）**。
公式100問で検証し、各項目に機械判定の可否・重要度・公式の実例を付けてある。
v1（`_review_criteria.md`・37項目）は履歴として残してあるだけ。

### 特に効く項目（2026-09-03 時点の到達状況）

| 項目 | 公式 | 自作(当初) | 自作(現在) | 内容 |
|---|---|---|---|---|
| O-3 | **資格別** | 5% | **下表を参照** | 正解と1点だけ違う誤答があるか（2026-09-09 に8資格を是正・SAP は不要と判定。下記） |
| 制約の裏返し | 0問 | 61問 | **0問** ✓ | 問題文の制約に真っ向から反する誤答 |
| キーワード直結 | 3% | 6% | **0%(2問)** ✓ | 問題文の語が正解肢にだけ出る |
| **肢どうしの語の重なり** | 資格別 | 0.154 | **9/11資格が公式レンジ内** | 4肢が軸を共有した集合になっているか |
| **選択肢が2文以上** | 資格別 | 21% | **8資格が不足** | 「操作A。操作B。」の組み合わせ形式 |
| O-2 | 14.6% | 0.4% | 未測定 | 2×2マトリクス構造か |
| S-9 | 1.1% | 0% | **着手済み** | 設定断片（JSON/ログ）を提示しているか |

**語の重なりは 9/1〜9/3 の3巡でほぼ解消した**（残りは DOP の不足と AIF の超過のみ）。
効いた方法は **「正解肢の文をコピーして1〜2トークンだけ置換する」**。
意味的に骨格を似せるだけでは指標が動かないことを実測で確認している。

### その手順が O-3 を機械的に押し上げていた（2026-09-09 に判明）

「正解肢をコピーして1〜2トークン置換」は、**そのまま「正解と1点だけ違う誤答」を作る**。
語の重なりを直した副作用として **O-3 が全11資格で公式を超えた**。
上表の「42.3%」は 9/1 の値で、9/3 の作業後は測り直されていなかった（鉄則4の再発）。

O-3 は**資格ごとに公式値がまったく違う**（CLF-C02 9.2% / SAP-C02 61.3% と6倍以上）。
**全資格平均（公式30.2%）を基準にしないこと。**

| 資格 | 公式 | 自作 | 資格 | 公式 | 自作 |
|---|---|---|---|---|---|
| **DEA-C01** | 40.0% | **40.4%** ✓ | **MLA-C01** | 31.7% | **35.3%** ✓ |
| SAA-C03 | 26.2% | 62.7% | SCS-C03 | 30.6% | 60.6% |
| **CLF-C02** | 9.2% | **10.5%** ✓ | **SOA-C03** | 24.6% | **26.2%** ✓ |
| **DOP-C02** | 53.3% | **56.8%** ✓ | **SAP-C02** | 61.3% | **74.3%** ✓(手付かず) |
| **DVA-C02** | 36.9% | **36.8%** ✓ | **AIP-C01** | 20.5% | **22.1%** ✓ |
| **AIF-C01** | 17.2% | **19.3%** ✓ | | | |

**是正済みは DEA-C01 / CLF-C02 / SOA-C03 / DVA-C02 / DOP-C02 / AIP-C01 / MLA-C01 / AIF-C01**（いずれも 2026-09-09。双子肢を D4/D6/D1 の別型の誤答に差し替え）。

**SAP-C02 は測り直した結果レンジ内で、是正しないと判断した**（2026-09-09）。
公式 61.3% に対し自作 74.3%（比1.21）で、`audit_criteria.py` の C2-3（閾値0.6・±10pt）でも
公式80.0%/自作87.6% と緑。下げれば公式を割って逆の手がかりになるうえ、
**語の重なりの余裕が 0.029 しかない**（0.397 / 下限0.3681）。**やらない判断も成果**。
詳細は `資料/生成/_予測_sap_aip_o3_20260909.md`。
なお `SAP-C02_orig.json` は**無改変ラウンドトリップがバイト一致しない**（CRLF と LF が混在し
末尾改行だけ LF）。触ると全行が差分になるので、書き戻す前に必ず確認すること。

**AIP-C01 を `data/orig.js` で測るときは `set=orig` で絞ること（鉄則5）。**
AIP だけが一問一答221問（`set=flash`）を同じプールに持っており、
選択肢が1行の用語なので O-3 は 0.9% しか出ない。**全体で平均すると 22.1% が 9.6% に化ける。**
実際に 2026-09-09、これで「AIP は公式19.6%に対し9.6%で不足10pt」という誤った報告が出た。
公式模試に一問一答形式は無いので比較自体が成立しない
（`audit_explanations.py` が一問一答を別扱いにしているのと同じ理由）。他10資格に flash セットは無い。
手順と予測・実測の照合は `資料/生成/_予測_dea_o3_20260909.md`、`_予測_clf_soa_o3_20260909.md`、
`_予測_dva_dop_o3_20260909.md`（DVA 31問×1肢で 64.0%→36.8% / DOP 22問×1肢＋5問×2肢で 79.7%→56.8%）、
`_予測_mla_aif_o3_20260909.md`（MLA 28問×1肢で 59.5%→35.3% / AIF 6問×1肢で 24.6%→19.3%）。

**「乖離が小さいから是正不要」は比で判断する。AIF-C01 は +7.4pt でも比 1.43 で外れていた**（2026-09-09）。
pt 差で見ると AIF は SAP-C02（+13pt・比1.21・見送り）より小さいのに、
公式値が 17.2% と低いぶん**比では 1.43 で目標レンジ (x0.75〜x1.25) の外**だった。
あわせて、AIF は**語の重なりが公式の上限を超えていた**（0.1733 / 上限 0.1732）ため、
是正のトレードオフ（重なりが下がる）がそのまま改善になる珍しいケースだった。
**是正するかどうかは「pt 差」ではなく「公式との比」と「重なりの余裕の向き」で決める。**

**直前に精読が入った問題を書き換えるときは、是正内容ごと消していないか照合する**（同上）。
`MLA-C01_orig_042` は精読で「バッチ変換は前処理に使えない」という断定を
「使えるが推論コンテナとしての用意が要る」に直したばかりだったのに、
O-3 の初稿でその肢を別サービスに差し替えてしまい、**是正した知識ごと消えるところだった**。
**是正後の記述そのものを誤答の内容にする**書き方に変えて両立させた（0.895→0.556）。

**資格ごとに「残してよい双子」を先に決めること。** CLF-C02 は公式 9.2% と極端に低いが、
公式にも 12/130 問はある。ゼロにすると逆の手がかりになるので、
**ゲスト OS とホスト OS、RPO と RTO、SNS と SQS のような「その資格の核心を問う対比」を
意図的に残す枠として数え**、残りを書き換える設計にした（CLF 46問中 12問を温存して 34問を是正）。
SOA-C03 では 2×2 マトリクス構造（判断×行動）の問題も温存対象にした。潰すと O-2 が減る。

**下げるときは肢どうしの語の重なりとのトレードオフを先に測る。**
双子肢を別型に書き換えると重なりは必ず下がる（DEA では素朴に書くと1問あたり 0.090 低下）。
**書き換える肢に、他の誤答肢と共通する語彙を意図的に残す**と 0.055 まで半減できた。
それでも DEA は 0.332→0.317（★不足の下限 0.3143）まで詰まり、**これ以上は下げられない**。
着手前に「その資格の重なりが公式値からどれだけ余裕があるか」を必ず測ること。

**「語彙を残す」は勘でやると外れる。語単位で採点しながら書くこと（2026-09-09・SOA-C03）。**
SOA は重なりの余裕が 0.014 しかなく、初稿は 20問で 0.014 を使い切って下限に触れた
（1問あたり −0.115）。原因は語単位で見ると2つだけで、
**(1) 元の肢が他肢と共有していた語を落とす、(2) その肢にしか出ない語を新しく持ち込む**。
`tools/_o3b_tok.py` がこの2つを語のリストで表示し、`tools/_o3b_cand.py` が
候補文ごとに「正解肢との最大類似 / 重なりの差分 / 文数 / 孤立語」を一括採点する。

**文字列の並びだけ崩せば、語の集合は残せる（2026-09-09・DVA/DOP）。**
O-3 は SequenceMatcher（文字の並び）、語の重なりは Jaccard（語の集合）で測っている。
**節の順序を入れ替える・同じ末尾の一文をそのまま残す**と、語の集合を保ったまま並びだけ崩れる。
DVA の初稿（素朴に別サービスへ差し替え）は 1問あたり −0.120 だったが、この書き方で −0.05〜0.06 になった。

**誤答を書き換えると「キーワード直結」が新しく生まれる（同上）。**
問題文の特徴語（`Amazon CloudWatch Logs` / `AWS RAM`）が全誤答肢から消えて正解肢だけに残る問題が
DOP で2問できた。`audit_strawman.py` の keyword_leak で検出できる。**O-3 の是正では毎回通すこと。**
候補を2〜3案書いて採点し直す運用に変えたところ、**1問あたり −0.115 → −0.024 になり、
38問を書き換えても 0.245→0.236 で収まった**（下限 0.2313）。
**重要な性質: 重なりは語の集合、O-3 は文字列の並びで測る。**
語彙を保ったまま語順と構文を変えれば、重なりをほぼ落とさずに O-3 だけ下げられる。

**残る本丸は「選択肢が2文以上」**（8資格で不足）。これは誤答だけでは動かない。
公式で多いのは正解肢自体が「操作A。操作B。」の2文構造だからで、
着手するなら鉄則3の手順で**正解肢を含めて**組み直すことになる。

測るコマンド:
```
python tools/audit_strawman.py   制約の裏返し・キーワード直結・手動が正解になる問題
python tools/audit_pattern.py    文の組み立て・肢どうしの語の重なり・評価軸の位置
```

---

## 鉄則3: `id` は変えない。それ以外は理由があれば変えてよい

**変えてはいけないのは `id` だけ。** `id` が変わると利用者の学習履歴
（正答率・習得状態・復習予定）が切れる。問題を差し替えるときも、内容が同じ論点なら `id` は維持する。

それ以外のフィールドは、**理由があれば変えてよい**。ただし変更の重さが違う。

| 変更 | 扱い |
|---|---|
| `options[].text`（誤答肢）/ `explanation` | 通常の修正。ここが作業の中心 |
| `question` | 明確な理由があるとき（キーワード直結を潰す等）。分量は維持する |
| `domain` / `level` / `n_correct` / 選択肢の増減 | 出題の形を直すときに変える。検査（`audit_content.py` / `audit_domains.py`）を必ず通す |
| **`options[].correct`（正解の割り当て）/ 正解肢の `text`** | **その問題の正解が実際に不適切なときは変えてよい。** ただし下記を守る |

### 正解を変えるときの手順

以前は「正解は絶対に変えない」というルールにしていたが、**これは有害だった**。
正解そのものが不適切な問題を直せなくなり、「正解が本当に最適解か」が未検証のまま残った。

変えるときは:
1. **なぜ現在の正解が不適切なのかを、公式ドキュメントの根拠つきで記録する**
2. 解説を新しい正解に合わせて書き直す（`audit_consistency.py` で矛盾0を確認）
3. その問題について、これまでの検証をやり直す（分量・当てやすさ・誤答の型）
4. `id` は変えない（履歴を切らないため）

**一括処理で正解を変えてはいけない。** 1問ずつ根拠を確認すること。
まとめて正解を書き換えると、どれが正しくてどれが間違いか分からなくなる。

### 安全装置

`tools/{資格}_apply_patch.py` は**正解肢への書き込みを検出したら1件も書かずに中断する**。
誤答肢だけを直す作業（大半はこれ）では、この安全装置を使うこと。
実際にこれで選択肢の取り違えを防いだ実績がある。

作業前に `tools/{資格}_snapshot.py` で不変項目を保存し、
終わったら `tools/{資格}_verify.py` で照合する。

### 安全装置を使う

`tools/{資格}_apply_patch.py` は**正解肢への書き込みを検出したら1件も書かずに中断する**。
実際にこれで選択肢の取り違えを防いだ実績がある。新しい資格用は既存のものをコピーして作る。

作業前に `tools/{資格}_snapshot.py` で不変項目を保存し、
終わったら `tools/{資格}_verify.py` で照合すること。

---

## 鉄則4: 直したら全部の観点で測る。ただし観点が足りているかも疑う

```
python tools/audit_all.py       全観点を一括実行
python tools/audit_criteria.py  チェックリスト58項目の充足状況
```

**個別の指標だけ見て「直った」と判断しないこと。** 1つ直すと別の症状が出る。

### 「公式より低ければ良い」は間違い。**公式の値に合わせる**

実際にこれで失敗した。DVA-C02 の「正解が最長」を 58% → 11% まで下げたところ、
**最長の肢が正解である割合が 4.9% になり、公式 23.5% を大きく下回った**。
「一番長い肢を捨てれば当たる」という逆の手がかりを作っていた。

公式は正解が最長の問題を 2割強もつ。これは欠陥ではなく自然な分布であり、
ゼロに近づけるほど良いわけではない。**上下どちらにずれても手がかりになる。**

対処として、長さ調整で足していた末尾の句を16問から削り、20.6% に戻した。
測るときは公式との差の**絶対値**を見ること:

```
python -X utf8 -c "
import json
js=open('data/orig.js',encoding='utf-8').read(); n=json.loads(js[js.index('['):js.rindex(']')+1])
off=json.load(open('資料/変換済み/questions_all.json',encoding='utf-8'))['questions']
def stat(qs,label):
    hi=lo=same=0
    for q in qs:
        op=q.get('options') or []
        cor=[len(o['text']) for o in op if o.get('correct')]; wr=[len(o['text']) for o in op if not o.get('correct')]
        if not cor or not wr: continue
        m=max(cor+wr); a=max(cor)==m; b=max(wr)==m
        if a and not b: hi+=1
        elif b and not a: lo+=1
        else: same+=1
    t=hi+lo+same
    if t: print('%-16s 最長が正解 %5.1f%%  最長が誤答 %5.1f%%'%(label,hi/t*100,lo/t*100))
for ex in ['DVA-C02','MLA-C01']:
    stat([q for q in off if q.get('exam')==ex], ex+' 公式')
    stat([q for q in n if q.get('exam')==ex and q.get('set')=='orig'], ex+' 自作')
"
```

**修正の前に、その資格が本当に外れているかを測ること。** MLA-C01 は
24問が「正解が最長」に該当していたが、分布で見ると 26.0% で既に公式 21.0% 並みだった。
直していれば DVA-C02 と同じ逆の癖を作っていた。

### 検査ツール自体が「絶対値で判定」していないか疑う

**2026-08-29 の点検で、公式と比べずに絶対値で判定していた検査が4つ見つかった。**
いずれも健全な状態を欠陥として報告しており、指示された側が不要な作り直しに入る危険があった。

| 検査 | 誤った判定 | 実際 |
|---|---|---|
| サービスの偏り | `share>=30` で警告 | 公式 41% に対し自作 33% の DEA-C01 を「偏りが大きい」と報告 |
| 正解の構成が同じ問題 | 1組でもあれば警告 | 公式にも同じ現象(1000問あたり 公式12.1組/自作16.5組) |
| 問題文の下限割れ | 1問でもあれば警告 | 公式は**定義上10%**が下位10%を下回る。自作は0.6% |
| 正解の位置(データ上)・問題数 | 常時「要確認」 | どちらも実害がなく、本当の問題が埋もれていた |
| 選択数の明記 (C5-5, 2026-09-01) | 自作だけ絶対値で判定し222問を警告 | 公式も**85%が未記載**(160/175問)。アプリのUIは「Nつ選択」タグを常時表示しており実害なし |

**古い測定値を放置しない、も同類の失敗。** HANDOFF に残っていた「O-3 は 5.2%」を信じて
再着手しかけたが、実測すると 42.3%（公式 30.5%）で**とっくに完了していた**。
数値を引用する作業を始める前に、必ずその場で測り直すこと（2026-09-01。AIP の5肢残作業でも同じことが起きた）。

直した結果、`audit_all.py` の要確認は **4件 → 1件** になった。
残った1件(問題文の分量)だけが実際の乖離だった。

**「不足」だけを見る検査は、半分しか見ていない。** 2026-09-03 に判明した実例:
`audit_pattern.py` は資格ごとの比較で不足しか警告しておらず、AIF-C01 の語の重なりが
公式 0.157 に対し自作 0.212（135%）と**超えている**のを検出できていなかった。
超過も公式との乖離であり、不自然な手がかりになる。上下どちらも警告するようにした。

**新問を作るときは、既存改善用の手順をそのまま流用しない。** 同じく 2026-09-03 の実例:
既存問題の重なりを上げるために確立した「正解肢をコピーして1〜2トークン置換」を
新問作成にも使ったところ、新問の重なりが 0.382（公式 0.257）になった。
改善作業では低い値を引き上げるので有効だが、ゼロから作ると最初から高く出て行き過ぎる。
**新問には「公式の分布（平均だけでなく散らばり）に合わせる」を明示すること。**
新問は `python tools/_verify_newq.py` で検証する（id衝突・重なり・分量・キーワード直結を一括）。

**総合検査に入っていない観点は、無いのと同じ。** 語の重なりは「公式との品質差の本丸」なのに
`audit_all.py` から漏れており、`audit_pattern.py` を個別に叩かないと気づけなかった
（2026-09-03 に統合）。新しい検査を作ったら `audit_all.py` に必ず登録する。

**新しく検査を書くときは、必ず公式側にも同じ検査をかけて基準を決めること。**
「この値なら問題だろう」という直感で閾値を置かない。

`audit_all.py` は**機械で測れない観点も「未検査」として必ず表示する**。
検査が全部通っても品質は保証されない。

### 原因を数えずに、測りやすい原因に飛びつかない

**2026-09-06 に実際にやった失敗。** ブラインドテストで「自作は表面的手がかりだけで
解ける率(strong)が公式60%に対し90%」と判明したとき、判定者は理由として
「制約との明確な矛盾」を最も多く挙げていた。にもかかわらず、**機械で測りやすい
「1トークン違いの双子の肢」に飛びついて**是正した。

結果: 双子は 24%→13% に減ったが、**strong率は 90%→87% しか下がらなかった**。
後から数え直すと、双子は strong 26問中7問(27%)にすぎず、
最多は「問題文の制約語をそのまま反復する正解肢」の11問だった。

**教訓: 修正に着手する前に、原因別の件数を数える。**
鉄則8は「効果を予測して後で照合する」だが、予測の前に**原因の切り分け**が要る。
予測だけ立てても、直す対象が主因でなければ当たらない。

あわせて分かったこと（機械で測って公式と比べた結果、対処不要と判明したもの）:
- 問題文の語が正解肢にだけ出る … 公式37% / 自作12%（**自作の方が少ない**）
- 極端表現(「手動で」「一切〜ない」)を含む誤答 … 公式1.4% / 自作1.0%（同上）
**判定者のコメントを鵜呑みにせず、必ず公式と比較して数える。**

### 観点そのものに抜けがないかを定期的に確認する

**これが最大の失敗だった。** 指摘を受けるたびに観点を後追いで足しており、
`audit_all.py` はチェックリスト58項目のうち13項目しか測っていなかった時期がある。

観点は**公式模試を読んで導く**。思いついた順に足してはいけない。

```
python tools/audit_criteria.py   ← 58項目それぞれが「測定済み / 要AI / 未実装」のどれかを表示する
```

「未実装」が増えていたら、機械化するか、AIに読ませる手順を用意する。

### 個別の検査

| コマンド | 見るもの |
|---|---|
| `node tools/smoke_test.js` | ID重複・スキーマ |
| `python tools/audit_consistency.py` | 解説と正誤フラグの矛盾 |
| `python tools/audit_domains.py` | ドメイン配分が公式試験ガイドと合うか |
| `python tools/audit_explanations.py` | 解説の分量 |
| `python tools/audit_difficulty.py` | 当てやすさ（正解が最長、キーワード一致など） |
| `python tools/audit_content.py` | 重複・出題の形・古い情報・偏り |
| `python tools/build_orig_data.py` | `資料/生成/*.json` → `data/orig.js` のビルド |

---

## 鉄則5: 測り方そのものを疑う

**測定の失敗で2回、誤った結論を出した。**

1. 公式模試30問と自作30問を別々のサンプル・別々の判定者で比べ、
   「改善していない」と報告した。→ 公式側が30%→46%とぶれており、比較になっていなかった。
   **同じ問題の改善前後をペアで比べたら、24/27→14/27と実際は改善していた。**
2. サービス名の照合で「Amazon CloudWatch」と「CloudWatch」を別物として数え、
   カバー済みの領域を「未カバー」と誤検出した。

### 測るときの決まり

- **前後比較は、同じ対象・同じ判定者で行う**（別サンプルでの比較は誤差に埋もれる）
- **機械的な検出結果は、必ず実データで裏を取る**（ありもしない問題を追いかけることになる）
- **資格をまたいで基準を作らない**（CLF-C02の公式は中央74字、SAP-C02は353字。4倍以上違う）
  - **これは3回踏んでいる。** 分量で1回、選択肢の語の重なりで2回目。
    語の重なりの公式値は SAP-C02 0.409 / DOP-C02 0.438 に対し **CLF-C02 0.119 / AIF-C01 0.157** と
    3倍以上開く。全体平均(0.267)を目標に置いたため、
    **すでに公式を上回っていた CLF-C02 に、水増ししないと届かない目標を指示してしまった**。
  - 対策として `audit_pattern.py` は主要項目を**必ず資格ごとに**表示する。
    全体平均だけを見て目標を決めないこと
- 平均だけでなく**分布**を見る（平均は到達していても、極端に短いものが107個残っていた）

### 直し方も資格ごとに選ぶ。他資格で成功した手順をそのまま流用しない

2026-09-09、複文率（選択肢が2文以上）を7資格で公式水準に合わせたときの教訓。

私は「SCS-C03 で成功した加筆方式（『操作。結果・適用先・条件』に書き直す）を手本に」と
全担当に指示したが、**これは SCS だけに通じる手だった**。

- **加筆方式は肢どうしの語の重なりを必ず下げる。** 重なりは Jaccard（共通語÷全語）なので、
  肢ごとに別語彙で第2文を足すと共通語が増えないまま全語だけ増える。
  SAP-C02 の担当が dry-run で全バッチの低下（0.401→0.344 等）を検出し、
  そのまま進めれば 0.376（公式比92%）まで壊すところだった
- SCS で使えたのは、**SCS の重なりに上げ代があったから**（当時0.276 / 公式0.285）
- **既定は分割方式**: 既存の連用中止を句点に替えるだけ（「XをAし、YをBする。」→
  「XをAする。YをBする。」）。語彙集合を1語も増減させないので重なりが原理的に動かない。
  DEA/DOP/SOA/DVA/SAP の5資格がこれで達成し、**重なりは小数第3位まで完全不変**だった
- 肢が公式より長い資格（SOA 72字/公式67字、DVA 69字/公式57字）では、
  加筆すると分量が公式から離れる。**分割一択**

**組み直すと「最長が正解」の分類が問単位で反転する。** DEA で4問、DOP で1問発生した。
適用前に dry-run で hi/lo/same が組み直し前と一致するか確認すること。

### 二値の指標は「満たしたのに近づいていない」を隠す

同じ作業で SAP-C02 の担当が見つけた。**複文率は「2文以上か否か」でしか測っていない。**

| 文数 | 1文 | 2文 | 3文 | 4文 | 5文以上 |
|---|---|---|---|---|---|
| 公式SAP | 48 | 103 | 95 | 41 | 26 |
| 自作SAP | 80 | 355 | 11 | 0 | 0 |

「2文以上」は 82%（公式84%）で**指標は緑になったが、公式の3文以上52%に対し自作は2%**。
分布の裾がまったく再現できていない。**この検査では検出されない。**
指標が緑になったことと、公式の書き方に近づいたことは別。

---

## 鉄則6: 事実の正確性は最優先

誤答肢に書く内容も、**AWSの仕様として実在し、説明が正しい**こと。
もっともらしくするために存在しない機能を書いてはいけない。
実際に `RetainSharingOnAccountLeaveOrganization` という存在しないパラメータを
実在するかのように書いた事故があった。

### これはアプリのコードにも当てはまる

問題データには厳格に適用していたのに、**アプリのコードには適用していなかった**。
モデル名を確認せずに `gpt-4o` と書き、2年近く古いモデルを利用者に選ばせるところだった
（当時の最新は GPT-5.6）。

**モデル名・料金・サービスの上限は変わる。** アプリに固定で持たせると、
古い情報を利用者に強制することになる。自由入力にするか、提供元のドキュメントを見るよう促すこと。

### 誤りが出やすいのは「列挙」

これまで見つかった実際の誤りは**ほぼすべて列挙**だった。

- S3イベント通知の送信先から EventBridge が抜けていた
- Trusted Advisor のカテゴリが5つになっていた（正しくは6つ）
- CloudWatch高解像度アラームの期間から 20秒 が抜けていた
- Bedrockガードレールのポリシーから 自動推論チェック が抜けていた

**列挙するときは必ず aws-docs MCP で全項目を確認する。**自信がなければ「など」で開く。
料金・上限値・リージョン数など変わりやすい数値は書かない。

**1件見つけたら、全問を横断して同じ誤りを直すこと。**
上記3件は指摘が6箇所だったが、正規表現で全問走査したら8箇所あった。

### 「その機能はあるか」で止めず、「後から変えられるか」まで見る

2026-09-07 に見つけた誤りの型。**新問を作るときに aws-docs で裏取りしていたのに、
確認が「その機能は存在するか」で止まっていた**ため、次を書いていた:

- Bedrock ナレッジベースの **chunkingConfiguration とパーサーは、データソース作成後に変更できない**
  （`API_agent_UpdateDataSource` と `kb-data-source-customize-ingestion` に明記）のに、
  「設定を変更して再同期する」という**成立しない手順**を正解や誤答にしていた（7問）
- あわせて `chunkingStrategy` に **`カスタム` という存在しない値**を書いていた
  （実在するのは NONE / FIXED_SIZE / HIERARCHICAL / SEMANTIC のみ。カスタム分割は
  「チャンキングなし」＋ `customTransformationConfiguration` で Lambda を挟む）

**マネージドサービスの設定を触る問題を作ったら、Update系APIのドキュメントで
"You can't change X after ..." を検索すること。**

**この型は AIP 以外にもあった。** 2026-09-09 の全資格横断調査で、
DynamoDB のキースキーマ（作成時に固定。`UpdateTable` に `KeySchema` がない）を
既存テーブルで差し替える前提の問題が SAA/SOA/DVA/DEA に4問、
一問一答にも Bedrock ナレッジベースの埋め込みモデル変更が1問あった
（`UpdateKnowledgeBase` で変えられるのは name/description/roleArn のみ）。

**この観点は双方向に効く。**「変えられないのに変えると書く」誤りと、
**「変えられるのに『変えられません』と書く」誤り**は同じ確認で見つかる。
2026-09-09 の全資格横断調査では、後者が MLA-C01_orig_100 で見つかった
（SageMaker ドメインのネットワークモードは `UpdateDomain` で変更できる。
制約は「InService/Pending/Deleting のアプリがないこと」だけ）。
Flows・API Gateway に続く3件目で、**この型は「陳腐化した断定」と根が同じ**。

### 新問は「作成時の裏取り」だけでは足りない。精読を通すこと

上記7問のうち**3問は、作成時に MCP で裏取りしたはずの新問**だった。
作成時の裏取りは自分が書こうとしていることの確認になりがちで、
**書かなかった前提（変更可否・値の一覧）は視野に入らない**。
新問も既存問と同じ精読ラウンドに通すこと。`_verify_newq.py` は機械的な検査で、
事実の正確性は見ていない。

---

## 鉄則7: 途中で消えても成果が残るように作業する

セッションの上限で作業が中断する事故が何度も起きた。

- **10問処理するごとに必ずファイルへ保存する**
- **既存ファイルを別のIDで上書きしない**（後から作った25問が既存を上書きし、消えかけた）
- **調べたことも途中で保存する**。
  「公式問題を読んで論点を確認 → 最初の1問を書く」の間に通信が切れ、
  1〜2時間分の調査が丸ごと消えた事故がある。
  問題を書き始める前に、**調べた内容をメモファイルに落としてから**作業に入ること
  （例: `資料/生成/_調査_{資格}.md`）。次の担当者はそこから再開できる

> 以前は「サブエージェントを多数起動しない」というルールも置いていたが、**削除した**。
> 実際には5〜6体を並行させても問題なく完走しており、根拠のない制限だった。
> 起動数より、**同じファイルを複数の作業が同時に触らないこと**が重要。

### 並行して作業させるときは、資格単位で分ける

実際に、2つの作業が同じ `資料/生成/*_orig*.json` を同時に編集した。
どちらもファイルを読み直してから書く短いトランザクションだったため実害はなかったが、
**片方の書き込みがもう片方に上書きされる危険があった**。

分けるときは**担当する資格を明示し、他資格には触らないよう指示する**こと。
横断的な作業（全資格が対象）を2つ同時に走らせない。
どうしても必要なら、対象IDのリストを事前に排他的に分割して渡す。

---

## 鉄則8: 修正の前に「何がどれだけ良くなるか」を数値で予測し、後で照合する

**これを怠ったのが、遠回りした最大の原因だった。**

「正解が最長」を99%→17%に直したが、**消去法で解ける割合はほとんど変わらなかった**。
先に「この修正で消去法率が何%下がるはずか」を予測していれば、
効かない修正に時間を使う前に気づけた。

修正に着手する前に:
1. **今の数値**を測る（`audit_criteria.py` の該当項目）
2. **修正後にどうなるはずか**を書き出す
3. 修正後に**実際の数値**と照合し、予測と違ったら**なぜ違ったかを考えてから次に進む**

予測と結果がずれたときは、たいてい**測っている指標が的外れ**。
指標を足すか差し替える（`tools/audit_*.py` を直す）。

---

## 環境メモ

- Windows。**日本語を含むファイルの書き込みに PowerShell の `Set-Content` を使わない**（文字化けする）。
  Write ツールか Python を使うこと。これは2回踏んでいる
- PowerShellのヒアドキュメント内でPythonコードを書くと、引用符の扱いで壊れやすい。
  複雑な置換は Edit ツールを使う
- Python は `python`、Node は `node`。`$env:PYTHONIOENCODING="utf-8"` を付けないと文字化けする
- `python ... | Select-Object -First 3` のように出力を絞ると、
  それ以上出力しようとした時点でプロセスが落ちる（音声生成を止めてしまった）
- `資料/` と `audio/` は `.gitignore` 済み。**著作権対応のためこの方針は崩さない**
  （公式問題はサイトに同梱しない。利用者が手元のファイルを読み込む方式）
- **問題JSONの保存形式はファイルごとに違う。**「indent=2 + CRLF + 末尾改行」で統一されてはいない。
  実測（2026-09-09・全70ファイル）: indent1/CRLF/末尾改行あり 42、indent1/LF/末尾改行あり 12、
  indent2/CRLF/末尾改行なし 12、indent1/CRLF/末尾改行なし 4。
  **書式を指定せず「無改変ラウンドトリップがバイト一致すること」を条件にすること。**
  形式を揃えにいくと全行が差分になり、何を直したのか読めなくなる
  （2026-09-09 に私が「indent=2+CRLF+末尾改行で保存」と指示し、担当3人が実データと違うと指摘してきた）

---

## 公開とデータの扱い

- 公開先: https://t03-77.github.io/quiz-demo-x7k2m/
- push は `git push origin main` で通る（認証は保存済み）
- **差し替えるファイルを Service Worker にキャッシュさせない。**
  デモ用キーを作り直したのに、利用者のブラウザが古い `data/demo_key.js` を返し、
  新しいパスワードで解錠できなくなった。`sw.js` の fetch は cache-first のため、
  リストに書いていないファイルも一度読めば残り続ける。
  （デモモード自体は 2026-08-31 に役目を終えて削除したが、この教訓は残す。
  差し替える前提のファイルを新設するなら `sw.js` で素通しにし、読み込み側に `?t=` を付けること）
  **「push したのに反映されない」と言われたら、まず公開ファイルの中身を取得して比べること。**
  今回は公開ファイル自体は正しく、原因はキャッシュだった
- **`index.html` を直したら `sw.js` の `VERSION` を上げる。** `index.html` はキャッシュ対象なので、
  上げないと既存の利用者には古い画面が出続ける
- **公式問題を改変したものをサイトに載せない。** 翻案にあたり著作権上の問題がある。
  論点を参考にして別のシナリオで作るのは問題ない（アイデアは著作権の保護対象外）
